# ==============================================================================
# ROHAN — Environment Configuration Template
# ==============================================================================
# Copy this file to '.env' and fill in your values.
# Do NOT commit the actual '.env' file to version control.

# --- LLM API Keys -------------------------------------------------------------
# Required if using the autonomous refinement loop or analysis features.
OPENROUTER_API_KEY=...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=...

# --- LLM Settings (Prefix: LLM_) ----------------------------------------------
# Provider: "openrouter" | "openai" | "google"
LLM_PROVIDER=openrouter

# Defaults models for each role (OpenRouter naming convention)
LLM_CODEGEN_MODEL=anthropic/claude-sonnet-4
LLM_ANALYSIS_MODEL=google/gemini-2.5-flash-lite
LLM_JUDGE_MODEL=google/gemini-2.0-flash-001

LLM_TEMPERATURE=0.2
LLM_MAX_TOKENS=4096

# --- Database Settings (Prefix: db_) ------------------------------------------
# Default is a local SQLite database for quickstart.
# For production/Docker use PostgreSQL: postgresql://user:password@localhost/rohan
DB_CONNECTION_STRING=sqlite:///./rohan.db
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20

# --- Simulation Settings (Prefix: sim_) ---------------------------------------
# General defaults for the ABIDES engine.
SIM_STDOUT_LOG_LEVEL=INFO
SIM_LOG_ORDERS=true
SIM_STARTING_CASH=10000000

# --- ABIDES Path (Optional) ---------------------------------------------------
# If using a specific local installation of ABIDES, set the path here.
# ABIDES_PATH=/path/to/abides
